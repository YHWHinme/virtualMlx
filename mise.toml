[tools]
uv = "latest"
ruff = "latest"

[tasks.go]
run = "uv run main.py"
description = "Run the VirtualMlx voice agent"

[tasks.lint]
run = "ruff check"
description = "Run the ruff linter"

[env]
STREAMING_MODE = "true"
